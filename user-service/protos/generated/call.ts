// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.32.1
// source: call.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "call";

export interface TempRequest {
  requestData: string;
}

export interface TempResponse {
  responseData: string;
}

function createBaseTempRequest(): TempRequest {
  return { requestData: "" };
}

export const TempRequest: MessageFns<TempRequest> = {
  encode(message: TempRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestData !== "") {
      writer.uint32(10).string(message.requestData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TempRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTempRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestData = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TempRequest {
    return { requestData: isSet(object.requestData) ? globalThis.String(object.requestData) : "" };
  },

  toJSON(message: TempRequest): unknown {
    const obj: any = {};
    if (message.requestData !== "") {
      obj.requestData = message.requestData;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TempRequest>, I>>(base?: I): TempRequest {
    return TempRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TempRequest>, I>>(object: I): TempRequest {
    const message = createBaseTempRequest();
    message.requestData = object.requestData ?? "";
    return message;
  },
};

function createBaseTempResponse(): TempResponse {
  return { responseData: "" };
}

export const TempResponse: MessageFns<TempResponse> = {
  encode(message: TempResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseData !== "") {
      writer.uint32(10).string(message.responseData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TempResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTempResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseData = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TempResponse {
    return { responseData: isSet(object.responseData) ? globalThis.String(object.responseData) : "" };
  },

  toJSON(message: TempResponse): unknown {
    const obj: any = {};
    if (message.responseData !== "") {
      obj.responseData = message.responseData;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TempResponse>, I>>(base?: I): TempResponse {
    return TempResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TempResponse>, I>>(object: I): TempResponse {
    const message = createBaseTempResponse();
    message.responseData = object.responseData ?? "";
    return message;
  },
};

export interface CallService {
  Temp(request: TempRequest): Promise<TempResponse>;
}

export const CallServiceServiceName = "call.CallService";
export class CallServiceClientImpl implements CallService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || CallServiceServiceName;
    this.rpc = rpc;
    this.Temp = this.Temp.bind(this);
  }
  Temp(request: TempRequest): Promise<TempResponse> {
    const data = TempRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Temp", data);
    return promise.then((data) => TempResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
